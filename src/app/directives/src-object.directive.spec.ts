import { SrcObjectDirective } from './src-object.directive';

describe('SrcObjectDirective', () => {
  it('should create an instance', () => {
    const directive = new SrcObjectDirective();
    expect(directive).toBeTruthy();
  });
});
